% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicadores.R
\name{indicadores}
\alias{indicadores}
\title{Calcular indicadores del mercado laboral con diseno muestral}
\usage{
indicadores(
  data,
  indicador,
  por = NULL,
  var_strata = NULL,
  var_psu = NULL,
  var_peso = NULL
)
}
\arguments{
\item{data}{\code{data.frame} con microdatos de EPEN (output de \code{\link[=descargar]{descargar()}}).}

\item{indicador}{Nombre del indicador a calcular:
\itemize{
\item \code{"ingreso_promedio"}: Ingreso laboral mensual medio (ocupados).
\item \code{"horas_promedio"}: Horas trabajadas promedio semanales (ocupados).
\item \code{"ingreso_mediano"}: Mediana del ingreso mensual (ocupados).
}}

\item{por}{Variable(s) de agrupacion: \code{"sex"}, \code{"region_code"},
\code{"education_code"}, \code{"institutional_sector_code"}, etc. \code{NULL} = nacional.}

\item{var_strata}{Nombre de la variable de estrato. Detecta automaticamente
\code{strata_code} (datos procesados) o \code{ESTRATO} (datos crudos INEI).}

\item{var_psu}{Nombre de la variable PSU. Detecta automaticamente
\code{cluster_id} (datos procesados) o \code{CONGLOME} (datos crudos INEI).}

\item{var_peso}{Nombre del factor de expansion. Detecta automaticamente
\code{weight} (procesados) o \code{FACTOR07} (datos crudos INEI).}
}
\value{
Un \code{data.frame} con el indicador calculado y error estandar,
agrupado segun \code{por}.
}
\description{
Calcula indicadores laborales incorporando el diseno muestral complejo de
la EPEN mediante el paquete \pkg{survey}. Los datos deben ser microdatos
procesados (output de \code{\link[=descargar]{descargar()}}) con al menos ocupados en la muestra.
}
\details{
Para obtener tasas de desempleo, actividad y empleo a nivel nacional o
regional, usa \code{\link[=resumen_nacional]{resumen_nacional()}}, que devuelve los indicadores
pre-calculados del pipeline INEI con la muestra completa de la PEA.
}
\examples{
# Ingreso promedio nacional con la muestra interna
indicadores(muestra_epen_2024, "ingreso_promedio")

# Ingreso promedio por sexo
indicadores(muestra_epen_2024, "ingreso_promedio", por = "sex")

\dontrun{
epen_2024 <- descargar(year = 2024)

# Ingreso promedio por departamento (codigo de region 1-25)
indicadores(epen_2024, "ingreso_promedio", por = "region_code")

# Horas trabajadas por sector institucional
indicadores(epen_2024, "horas_promedio", por = "institutional_sector_code")
}
}
\seealso{
\code{\link[=resumen_nacional]{resumen_nacional()}} para indicadores pre-calculados (incluyendo
tasas de desempleo y actividad). \link{indicadores_epen} para el dataset
interno con indicadores nacionales precalculados.
}
