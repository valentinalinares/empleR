% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel.R
\name{panel}
\alias{panel}
\title{Construir panel de viviendas entre trimestres moviles de la EPEN}
\usage{
panel(
  data_lista,
  vars_id = c("cluster_id", "dwelling_id", "household_id"),
  incluir_no_apareados = FALSE,
  labels_trimestre = NULL
)
}
\arguments{
\item{data_lista}{Lista de \code{data.frame} con microdatos de trimestres
consecutivos. Cada elemento debe ser el resultado de \code{\link[=descargar]{descargar()}} con
\code{variante = "lima_movil"}. El orden de la lista es el orden temporal.}

\item{vars_id}{Variables que identifican unicamente a una vivienda entre
trimestres. Por defecto: \code{c("cluster_id", "dwelling_id", "household_id")}.
Para datos crudos INEI usar: \code{c("CONGLOME", "VIVIENDA", "HOGAR")}.}

\item{incluir_no_apareados}{Logico. Si \code{FALSE} (por defecto), solo incluye
viviendas presentes en TODOS los trimestres. Si \code{TRUE}, incluye todas
las viviendas con datos en al menos un trimestre.}

\item{labels_trimestre}{Vector de etiquetas para identificar cada trimestre
en la columna \code{trimestre}. Si es \code{NULL}, usa "T1", "T2", etc.}
}
\value{
Un \code{data.frame} en formato long (una fila por vivienda-trimestre)
con columna \code{trimestre} indicando el periodo.
Atributo \code{tasa_apareamiento} con el porcentaje de viviendas apareadas.
}
\description{
La EPEN Lima Metropolitana tiene un diseno de panel rotante a nivel de
vivienda: cada vivienda es encuestada durante varios trimestres consecutivos
antes de salir del panel. Esta funcion identifica y enlaza las mismas
viviendas entre dos o mas trimestres, permitiendo analisis de transiciones
laborales.
}
\details{
La unidad de seguimiento en la EPEN es la \strong{vivienda} (no el individuo),
identificada por la combinacion \code{cluster_id + dwelling_id + household_id}.
El \code{person_id} no siempre esta disponible en los datos procesados.

El panel es aplicable principalmente a la variante \code{lima_movil}, donde
los trimestres se solapan (Ene-Feb-Mar, Feb-Mar-Abr, etc.) permitiendo
seguir las mismas viviendas mes a mes.
}
\examples{
\dontrun{
# Descargar tres trimestres consecutivos de Lima 2024
t1 <- descargar(2024, "lima_movil", "Trimestre Movil -(Ene-Feb-Mar)")
t2 <- descargar(2024, "lima_movil", "Trimestre Movil -(Feb-Mar-Abr)")
t3 <- descargar(2024, "lima_movil", "Trimestre Movil -(Mar-Abr-May)")

# Construir panel de viviendas
panel_q1 <- panel(
  list(t1, t2, t3),
  labels_trimestre = c("Ene-Feb-Mar", "Feb-Mar-Abr", "Mar-Abr-May")
)

# Ver tasa de apareamiento
attr(panel_q1, "tasa_apareamiento")

# Analizar transiciones: personas que cambiaron de estado laboral
library(dplyr)
panel_q1 |>
  select(cluster_id, dwelling_id, household_id, trimestre, labor_status_code) |>
  tidyr::pivot_wider(names_from = trimestre, values_from = labor_status_code)
}
}
